% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_Collection_of_poolingFunctions.R
\name{CellDEEP.Kmean}
\alias{CellDEEP.Kmean}
\title{K-means Based Cell Pooling for Seurat Objects}
\usage{
CellDEEP.Kmean(
  dataset,
  n_cells = 10,
  nstart = 100,
  assay_name = "RNA",
  readcounts = "mean",
  min_cells_per_subgroup = 25
)
}
\arguments{
\item{dataset}{A Seurat object. Must have PCA reductions calculated.}

\item{n_cells}{Integer. Target number of cells to pool into each pseudocell.}

\item{nstart}{Integer. Number of random sets to start with in \code{kmeans}.}

\item{assay_name}{Character. The assay to pull counts from (default "RNA").}

\item{readcounts}{Character. Aggregation method: "mean" (rounded average),
"sum", "10X" (mean * 10).}

\item{min_cells_per_subgroup}{Integer. Minimum cells required in each
sample-cluster subgroup to perform pooling (default 25).}
}
\value{
A new Seurat object where each "cell" is a pooled group of original cells.
}
\description{
Pools cells into "pseudocells" by applying k-means clustering to PCA embeddings.
This reduces data sparsity while maintaining the biological grouping of
sample, cluster, and condition.
}
\note{
This function requires that PCA has already been run on the input \code{dataset},
as it uses the "pca" reduction for clustering.
}
\examples{
# pooled_obj <- CellDEEP.Kmean(dataset = my_seurat, n_cells = 10, readcounts = "mean")
}
