% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_FindMarker.R
\name{FindMarker.CellDEEP}
\alias{FindMarker.CellDEEP}
\title{Differential Expression with Optional Cell Pooling}
\usage{
FindMarker.CellDEEP(
  object,
  ident.1 = NULL,
  ident.2 = NULL,
  group.by = NULL,
  test.use = "wilcox",
  Pool = FALSE,
  readcounts = "sum",
  n_cells = 10,
  assay = "RNA",
  cell_cutoff = 25,
  pool_way = "kmean",
  name.only = TRUE,
  logfc.threshold = 0.1,
  min.pct = 0.01,
  p_cutoff = 0.05,
  full_list = FALSE,
  ...
)
}
\arguments{
\item{object}{A Seurat object.}

\item{ident.1}{Character. The first identity group to compare.}

\item{ident.2}{Character. The second identity group to compare (optional).}

\item{group.by}{Character. The metadata column used for grouping cells.}

\item{test.use}{Character. DE test to use (default "wilcox").}

\item{Pool}{Logical. Whether to perform CellDEEP pooling before DE (default FALSE).}

\item{readcounts}{Character. Aggregation method for pools: "sum", "mean", or "10X".}

\item{n_cells}{Integer. Target number of cells per pool.}

\item{assay}{Character. Assay to use (default "RNA").}

\item{cell_cutoff}{Integer. Minimum cells per sample/cluster for pooling logic.}

\item{pool_way}{Character. Choice of "kmean" or "random" pooling.}

\item{name.only}{Logical. If TRUE, returns gene names only; otherwise, returns a data.frame.}

\item{logfc.threshold}{Numeric. Minimum log fold-change for DE filtering.}

\item{min.pct}{Numeric. Minimum detection rate for genes to be considered.}

\item{p_cutoff}{Numeric. P-value threshold for filtering (adjusted p-value).}

\item{full_list}{Logical. If TRUE, returns all genes regardless of p-value.}

\item{...}{Additional arguments passed to \code{Seurat::FindMarkers}.}
}
\value{
A vector of gene names or a data.frame containing DE statistics.
}
\description{
A high-level wrapper that performs Differential Expression (DE) analysis.
It can run standard Seurat DE or first aggregate cells into pseudocells
using K-means or Random pooling before performing the analysis.
}
\examples{
# Run standard Wilcoxon DE
# res <- FindMarker.CellDEEP(my_obj, ident.1 = "A", group.by = "group")

# Run DE with K-means pooling
# res <- FindMarker.CellDEEP(my_obj, ident.1 = "A", group.by = "group", Pool = TRUE, pool_way = "kmean")
}
